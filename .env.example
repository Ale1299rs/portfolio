# Copy to .env.local and fill in real values. Do not commit .env.local.

# DeepSeek Chat API key — used by /api/chat to power the site chatbot.
# Get one at https://platform.deepseek.com/
DEEPSEEK_API_KEY=
